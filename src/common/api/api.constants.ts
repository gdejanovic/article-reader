const baseUrl = 'https://jsonplaceholder.typicode.com';

export const apiRoutes = {
    postsUrl: `${baseUrl}/posts`,
    singlePostUrl : `${baseUrl}/post`,
    usersUrl: `${baseUrl}/users`,
    commentsUrl: `${baseUrl}/comments`
}